{% extends 'AdminMainBundle:Default:index.html.twig' %}

{%block page_title_h1 %}
    <h1 style="display: inline">
        {%trans%}APB_PAGE_DASHBOARD{%endtrans%}
        <a href="{{ path('_admin_main_homepage') }}" class="btn btn-primary">
            {%trans%}APB_BACK{%endtrans%}
        </a>
    </h1>
{%endblock page_title_h1 %}

{% block container %}
                
    {% block javascripts %}

            <script type="text/javascript" src="{{ asset('js/ckeditor/ckeditor.js') }}"></script>
            <script type="text/javascript" src="{{ asset('js/ckeditor/config.js') }}"></script>
            
            <script>
                
                $(document).ready(function() {
                    
                    CKEDITOR.replace( 'admin_pagebundle_staticpage_pageBody', { toolbar: 'Full' } );
                    
                });
                
            </script>

    {% endblock javascripts %}
    
    <br />
    
    <form action="{{ path('_adminpage_staticpage_save', { 'id': pageID }) }}" method="POST" {{ form_enctype(form) }} >
        {{ form_errors(form) }}
                
        {{ form_widget(form) }}
                    
        {{ form_rest(form) }}
            
        <input type="submit" value="{%trans%}APB_SAVE{%endtrans%}" class="btn btn-success">
        
    </form>
    
{% endblock container %}
